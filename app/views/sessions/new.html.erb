<%= form_tag("/signin", method: "post") do %>
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  Your Name: <%= collection_select(:user, :name, User.all, :name, :name) %><br />
  Your Password: <%= password_field_tag :password %></p>
  <%= submit_tag "Sign In" %>
<% end %>